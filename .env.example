# Copy to .env locally (never commit .env). On Vercel, set in Project → Settings → Environment Variables.
# Google AI Studio: https://aistudio.google.com/
GEMINI_API_KEY=
# İsteğe bağlı: varsayılan gemini-1.5-flash (kota/model uyumu için). Örn: GEMINI_MODEL=gemini-2.0-flash
# GEMINI_MODEL=
